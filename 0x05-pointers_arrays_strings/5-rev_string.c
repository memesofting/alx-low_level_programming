#include "main.h"
#include <string.h>
/**
 * rev_string - reverses strings
 *
 * Return: Always void (Success)
 */
void rev_string(char *s)
{
	char *rev;
	char a;
	int i;
	int len;

	len = strlen(rev);
	strdup(rev, s);
	for (i = len; i >= 0; i--)
	{
		a = rev[i];
		/*rev++;*/
	}
	/*_putchar(s[len]);*/
}
