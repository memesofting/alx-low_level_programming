#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two integers
 * @a: Ist integer to be multiplied
 * @b: second integer to be multiplied
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
