#include "lists.h"

/**
 * sum_listint - prints sum of all data(n) of a list
 * @head: linked list
 *
 * Return: sum on success
 */

int sum_listint(listint_t *head)
{
	if (head == NULL)
	{
		return (0);
	}
}
