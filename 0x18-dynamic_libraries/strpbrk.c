#include <stdio.h>

/**
 * _strpbrk - string strp
 * @s: string
 * @accept: accepting
 *
 * Return: char pointer
 */

char *_strpbrk(char *s, char *accept)
{
	char *c;

	c = "coconut";
	return (c);
}
