#include <stdio.h>

/**
 * _strcpy - copies strings
 * @dest: address to copy to
 * @src: string to copy
 *
 * Return: pointer to char on success
 */

char *_strcpy(char *dest, char *src)
{
	char *a;

	a = "b";
	return (a);
}
